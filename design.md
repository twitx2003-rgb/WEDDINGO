# Design System — "Aurora" 🌌

מערכת עיצוב שחולצה מעמוד הפורטפוליו של Mark Davis (UI/UX Designer): ערכה **כהה** עם חתימת גרדיאנט **ורוד → סגול → כחול**, כרטיסים כהים מעוגלים, ותגי אייקון זוהרים. המסמך הזה הוא מקור האמת לטוקנים, לטיפוגרפיה ולרכיבים. התצוגה החיה נמצאת ב-`design.html`.

> **התאמה לעברית:** כל שאר מערכת העיצוב (צבעים, מרווחים, רכיבים, רדיוסים, גרדיאנטים) נשמרה **כפי שהיא**. שונתה **רק** הטיפוגרפיה — הוחלפו הפונטים הלטיניים בפונטים עבריים שנבחרו בקפידה (ראה §2).

---

## 1. צבעים (Color Tokens)

### רקעים ומשטחים
| טוקן | ערך | שימוש |
|---|---|---|
| `--bg` | `#0b0a12` | רקע האפליקציה (כמעט-שחור בגוון סגול) |
| `--bg-sidebar` | `#0a0810` | רקע סרגל צד, כהה מעט יותר |
| `--surface` | `#16141f` | רקע כרטיסים |
| `--surface-2` | `#1e1b2b` | כרטיס מוגבה / שדות טופס |
| `--surface-3` | `#252236` | Hover על משטחים |
| `--border` | `rgba(255,255,255,.07)` | גבול עדין |
| `--border-strong` | `rgba(255,255,255,.13)` | גבול מודגש |

### טקסט
| טוקן | ערך | שימוש |
|---|---|---|
| `--text` | `#f6f5fb` | כותרות וטקסט ראשי |
| `--text-body` | `#b7b3c9` | גוף טקסט |
| `--text-muted` | `#89849c` | משני, כיתובים, מטא |

### חתימת הגרדיאנט
| טוקן | ערך |
|---|---|
| `--pink` | `#ec4899` |
| `--violet` | `#8b5cf6` |
| `--blue` | `#5b6ef5` |
| `--indigo` | `#6366f1` |
| `--gradient` | `linear-gradient(135deg, #ec4899 0%, #8b5cf6 52%, #5b6ef5 100%)` |
| `--gradient-soft` | גרסת רקע שקופה: `linear-gradient(135deg, rgba(236,72,153,.16), rgba(139,92,246,.16), rgba(91,110,245,.16))` |

**אקצנטים סמנטיים:** האקצנט הראשי הוא `--violet` (#8b5cf6). תוויות "eyebrow" משתמשות ב-`--pink`/`--violet`. הגרדיאנט שמור לכפתורים ראשיים, לתגי אייקון, ולפס הסטטיסטיקות.

---

## 2. טיפוגרפיה (Typography)

### הפונטים
המקור השתמש ב-**Poppins** (סאנס-סריף גיאומטרי, מודרני, פינות מעוגלות). לעברית נבחרו שני פונטים שנשמרים נאמנים לאופי הזה — משקל, אישיות, מודרניות וקריאות:

| תפקיד | פונט | למה זה מתאים |
|---|---|---|
| **תצוגה + כותרות + כפתורים** | **Rubik** | גיאומטרי עם פינות מעט מעוגלות — התאום העברי הקרוב ביותר ל-Poppins. מודרני, נקי, בעל מגוון משקלים עשיר (400–800). |
| **גוף + UI + כיתובים** | **Assistant** | סאנס עברי מודרני, ניטרלי וקריא במיוחד בגדלים קטנים. משתלב בהרמוניה עם Rubik (שני הפונטים תוכננו לעברית עכשווית). |

```css
--font-display: 'Rubik', 'Poppins', system-ui, sans-serif;   /* כותרות, כפתורים, מספרים */
--font-body:    'Assistant', system-ui, sans-serif;          /* גוף, UI, כיתובים */
```

הפונטים מוטמעים מקומית (`design-system/fonts/`, פורמט woff2) — בלי CDN, עובד אופליין.

### היררכיית הטיפוגרפיה
| סגנון | פונט | גודל (clamp) | משקל | Line-height | Letter-spacing |
|---|---|---|---|---|---|
| **Display** (שם הירו) | Rubik | `clamp(2.75rem, 6vw, 4.5rem)` | 700 | 1.05 | -0.02em |
| **H1** | Rubik | `clamp(2rem, 4vw, 2.75rem)` | 700 | 1.1 | -0.015em |
| **H2** (כותרת מקטע) | Rubik | `clamp(1.6rem, 3vw, 2.1rem)` | 700 | 1.15 | -0.01em |
| **H3** (כותרת כרטיס) | Rubik | `1.25rem` | 600 | 1.25 | normal |
| **Eyebrow / Overline** | Rubik | `0.8rem` | 600 | 1.4 | `0.08em`\* |
| **Body Large** (כותרת-משנה) | Assistant | `1.125rem` | 400 | 1.65 | normal |
| **Body** | Assistant | `1rem` | 400 | 1.6 | normal |
| **Caption / Small** | Assistant | `0.85rem` | 400 | 1.5 | normal |
| **Button** | Rubik | `0.95rem` | 600 | 1 | normal |
| **Nav item** | Assistant | `0.95rem` | 500 | 1 | normal |
| **Badge / Tag** | Rubik | `0.72rem` | 600 | 1 | `0.03em` |
| **Stat number** | Rubik | `clamp(1.75rem, 3vw, 2.25rem)` | 700 | 1 | normal |

\* **הערת RTL חשובה:** עברית לא "אוהבת" letter-spacing כמו לטינית, ואין בה אותיות רישיות. לכן ה-Eyebrow שבמקור היה `UPPERCASE` עם tracking רחב — בעברית מיושם כ**טקסט רגיל, משקל 600, צבע מהגרדיאנט, וגודל קטן**, עם letter-spacing מינימלי (0.08em) בלבד. זה משמר את ההיררכיה בלי לפגוע בקריאוּת.

---

## 3. מרווחים (Spacing) — בסיס 8px
`--space-1: 4px` · `--space-2: 8px` · `--space-3: 12px` · `--space-4: 16px` · `--space-5: 24px` · `--space-6: 32px` · `--space-7: 48px` · `--space-8: 64px` · `--space-9: 96px`

## 4. רדיוסים (Radius)
`--r-sm: 8px` · `--r-md: 12px` · `--r-lg: 16px` · `--r-xl: 22px` · `--r-pill: 999px`

## 5. צללים והילות (Elevation)
| טוקן | ערך | שימוש |
|---|---|---|
| `--shadow-card` | `0 4px 24px rgba(0,0,0,.35)` | כרטיסים |
| `--shadow-pop` | `0 12px 40px -12px rgba(0,0,0,.55)` | תפריטים צפים |
| `--glow` | `0 10px 30px -8px rgba(139,92,246,.5)` | כפתור ראשי / אלמנט גרדיאנט |

---

## 6. רכיבים (Components)

- **כפתור ראשי (Primary):** רקע `--gradient`, טקסט לבן, Rubik 600, רדיוס `--r-pill` או `--r-md`, `--glow`, אייקון חץ. Hover: הרמה `translateY(-2px)` + הגברת ההילה.
- **כפתור משני (Ghost/Outline):** רקע שקוף, גבול `--border-strong`, טקסט `--text`. Hover: גבול בצבע `--violet`.
- **כפתור אייקון עגול:** עיגול, גבול/גרדיאנט, אייקון בלבד (למשל Play).
- **Eyebrow:** תווית קצרה מעל כותרת מקטע, בצבע הגרדיאנט (ראה §2).
- **כרטיס שירות:** עיגול-אייקון עם גרדיאנט → כותרת H3 → תיאור Caption → קישור "עוד ←". גבול `--border`, רקע `--surface`, רדיוס `--r-lg`.
- **כרטיס פרויקט:** תמונה/תצוגה → תג (Badge) → כותרת → כותרת-משנה muted.
- **כרטיס המלצה:** סימן ציטוט גדול בגרדיאנט → טקסט → אווטאר + שם + תפקיד.
- **תג / Badge:** גלולה קטנה, רקע `--gradient-soft`, טקסט Rubik 600 קטן.
- **פריט סטטיסטיקה:** מספר גדול (Rubik 700) + תווית Caption. מוצג גם בתוך **פס גרדיאנט** רוחבי.
- **שדה טופס:** רקע `--surface-2`, גבול `--border`, רדיוס `--r-md`, טקסט `--text`, focus → גבול `--violet` + טבעת רכה.
- **פריט ניווט:** אייקון + תווית. מצב פעיל: רקע `--gradient-soft` + אינדיקטור גרדיאנט בקצה.
- **תג-אייקון (Icon Badge):** עיגול במילוי `--gradient`, אייקון לבן במרכז.

### עקרונות שימוש
1. **גרדיאנט = היררכיה.** משתמשים בגרדיאנט רק לפעולה/אלמנט אחד דומיננטי לכל מקטע — לא מפזרים.
2. **כהה תמיד, ניגוד גבוה לטקסט.** כותרות ב-`--text`, גוף ב-`--text-body`, מטא ב-`--text-muted`.
3. **רדיוס נדיב ועקבי.** כרטיסים `--r-lg`, כפתורים `--r-pill`/`--r-md`, תגים `--r-pill`.
4. **RTL:** להשתמש בתכונות לוגיות (`margin-inline`, `padding-inline`, `inset-inline`) כדי שהכול יתהפך נכון. אייקוני חץ מתהפכים לכיוון הקריאה.
