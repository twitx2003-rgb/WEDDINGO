# המחברת — אתר סיכומי הרצאות

אתר אישי **סטטי לחלוטין** (בלי build, בלי שרת, בלי API בתשלום) לסטודנט. המשתמש מצלם דפי הרצאה/שיעור בכתב יד, ו-**Claude Code עצמו הוא מנוע ה-AI**: הוא קורא את כתב היד, מתמלל, ובונה סיכום מובנה. האתר מציג הכל מאורגן לפי קורס ← הרצאה.

השפה: עברית, RTL. מתמטיקה: KaTeX (`$...$` inline, `$$...$$` display).

## מתי לעבד דפים

כשהמשתמש אומר **"תעבד את הדפים"**, "תסכם לי את ההרצאה", מדביק תמונות בצ'אט, או כשיש קבצי תמונה חדשים בתיקיית `inbox/` — הפעל את ה-skill **`process-notes`** (ראה `.claude/skills/process-notes/SKILL.md`) ופעל לפיו בדיוק.

אם קיים `inbox/job.json` (נוצר ממסך ה"סטודיו" באתר) — הוא מגדיר בדיוק את הקורס, ההרצאה והדפים לניתוח; פעל לפיו ומחק אותו בסיום (ראה שלב 0 ב-SKILL).

## חוקים קשיחים

- כל התוכן שנוצר — **בעברית**.
- התמלול **נאמן למקור**. לא ממציאים תוכן שלא כתוב. מילה לא קריאה → `[לא ברור: ניחוש?]`.
- מתמטיקה תמיד ב-KaTeX. לוודא ש-delimiters מאוזנים.
- מזהי קורס/הרצאה (`id`) הם **slugs ב-ASCII** (למשל `linear-algebra-1`, `2026-07-15-eigenvalues`). שמות התצוגה בעברית חיים ב-`content/manifest.json`.
- **נתיבים יחסיים בלבד** בקוד האתר (בלי `/` מוביל) — נדרש ל-GitHub Pages תחת subpath.
- כל עיבוד מעדכן את `content/manifest.json` **באותו קומיט** עם קבצי ההרצאה.
- פורמט הודעת קומיט לתוכן: `content: <שם הקורס> — <כותרת ההרצאה>`.
- לפני push, כשאפשר, לאמת שה-JSON תקין (`python3 -m json.tool content/manifest.json`) ושהאתר נטען.

## מבנה

```
index.html            SPA יחיד (ניתוב hash)
css/style.css         כל השפה העיצובית (טוקנים ב-:root)
js/app.js             ראוטר + רינדור + חיפוש + לייטבוקס
js/vendor/            marked + KaTeX (vendored, לא CDN)
content/manifest.json מקור אמת: קורסים ← הרצאות
content/courses/<course-id>/<lecture-id>/lecture.md + scans/
inbox/                תמונות גולמיות שממתינות לעיבוד
```

## הרצה מקומית לבדיקה

פתיחה ב-`file://` **לא** עובדת (fetch חסום). מריצים שרת סטטי:
```
python3 -m http.server 8000
```
ואז פותחים `http://localhost:8000/`.
